<div class="row">
  <div class="col-md-12">
    <h1>Категории %></h1>
  </div>
</div>

<table class="table table-striped">
  <tr>
    <th>Назвение</th>
    <th>Действия</th>
  </tr>

  <% @categories.each do |category| %>
      <tr>
        <td><%= link_to category.name, category_path(category) %></td>
        <td>
          <%= link_to 'Просмотреть', category_path(category) %>
        </td>
      </tr>
  <% end %>

</table>
